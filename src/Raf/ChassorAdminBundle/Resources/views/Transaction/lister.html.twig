{% extends "ChassorAdminBundle::admin-layout.html.twig" %}
 
{% block body %}

<h1>Les transactions</h1>

<table class="table table-striped table-hover table-condensed table-bordered">
<thead>
<tr>
    <th>Date</th>
    <th>Chassor</th>
    <th>Libellé</th>
    <th>Montant</th>
</tr>
</thead>
<tbody>
{% for tr in liste %}
<tr>
    <td>{{ tr.date|date("d/m/Y H:i") }}</td>
    <td>{{ tr.chassor.username }}</td>
    <td>{{ tr.libelle }}</td>
    <td>{{ tr.montant }}</td>
</tr>
{% endfor %}
</tbody>
</table>
 
{% endblock %}
